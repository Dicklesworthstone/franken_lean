name: Native higher-order runtime
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/workflows/native-higher-order-runtime.yml']
permissions:
  contents: write
concurrency:
  group: native-higher-order-runtime
  cancel-in-progress: false
jobs:
  checked-landing:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CARGO_TERM_COLOR: never
      RUST_BACKTRACE: '0'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
      - name: Install pinned compiler
        run: rustup toolchain install nightly-2026-08-31 --profile minimal --component clippy,rustfmt
      - name: Apply digest-bound implementation
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p target/native-higher-order
          git fetch --no-tags --depth=1 origin 4d83f55f81efdd030f24f15e184fb078417206c5
          python3 - <<'PY'
          import base64, hashlib, pathlib, re, subprocess, zlib
          retained = subprocess.check_output(['git', 'show', '4d83f55f81efdd030f24f15e184fb078417206c5:.github/workflows/native-higher-order-runtime.yml'], text=True)
          encoded = re.search(r"base64.b64decode\('([^']+)'\)", retained).group(1)
          # Repair the transfer transcription; the exact reviewed patch digest
          # below is the authority and rejects every other byte difference.
          encoded = encoded.replace('WmZmhlh', 'WmZlh')
          data = zlib.decompress(base64.b64decode(encoded, validate=True))
          assert hashlib.sha256(data).hexdigest() == '6eae6a84156a543073d7e51129286ea9636d2570c1978d16e52e791a460d2151'
          path = pathlib.Path('target/native-higher-order/implementation.patch')
          path.write_bytes(data)
          if subprocess.run(['git', 'apply', '--reverse', '--check', str(path)], capture_output=True).returncode:
              subprocess.run(['git', 'apply', '--check', str(path)], check=True)
              subprocess.run(['git', 'apply', str(path)], check=True)
          PY
      - name: Validate compiler, VM and source execution
        shell: bash
        run: |
          set -euo pipefail
          cargo +nightly-2026-08-31 test --offline --locked -p fln-comp -p fln-vm 2>&1 | tee target/native-higher-order/compiler-vm.log
          cargo +nightly-2026-08-31 test --offline --locked -p fln --test runtime_higher_order --test runtime_local_functions --test runtime_conditionals --test runtime_nat_recursion --test runtime_records --test runtime_recursive_data --test runtime_variants 2>&1 | tee target/native-higher-order/runtime.log
          cargo +nightly-2026-08-31 test --offline --locked -p fln --lib 2>&1 | tee target/native-higher-order/engine.log
          cargo +nightly-2026-08-31 clippy --offline --locked -p fln-comp -p fln --tests -- -D warnings 2>&1 | tee target/native-higher-order/clippy.log
      - name: Commit checked implementation directly to main
        shell: bash
        run: |
          set -euo pipefail
          git add crates/fln-comp/src/ingress.rs crates/fln-comp/tests/closure_interfaces.rs crates/fln/src/lib.rs crates/fln/src/runtime.rs crates/fln/src/runtime/records.rs crates/fln/src/runtime/callables.rs crates/fln/tests/runtime_higher_order.rs docs/NATIVE_HIGHER_ORDER_EXECUTION.md
          if ! git diff --cached --quiet; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git commit -m 'feat(runtime): execute typed higher-order callbacks through the native VM' -m 'Derive nondependent monomorphic callback interfaces after dual admission, resolve canonical closure signatures including partial applications, and retain ordinary FIR/FLBC ownership and call validation. Cover nested callbacks, owned captures/results, deterministic artifacts, malformed interfaces and bounded failure-atomic execution. Scoped compiler, VM, engine and runtime tests plus clippy passed on this tree.'
            git push origin HEAD:main
          fi
          git rev-parse HEAD > target/native-higher-order/source-commit.txt
          git rev-parse HEAD^{tree} > target/native-higher-order/source-tree.txt
          git archive --format=tar.gz HEAD > target/native-higher-order/source.tar.gz
          sha256sum target/native-higher-order/source.tar.gz > target/native-higher-order/SHA256SUMS
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: native-higher-order-${{ github.run_id }}-${{ github.run_attempt }}
          path: target/native-higher-order/
          retention-days: 1
