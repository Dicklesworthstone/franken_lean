[package]
name = "fln-vm"
version = "0.0.0"
edition = "2024"
license = "LicenseRef-MIT-OpenAI-Anthropic-Rider"
publish = false

[dependencies]

[dev-dependencies]
# The extern dispatch e2e's scratch roots route through the workspace fence
# (bead franken_lean-pw6t; edge declared in ci/WORKSPACE_GRAPH.txt).
fln-core = { path = "../fln-core" }
