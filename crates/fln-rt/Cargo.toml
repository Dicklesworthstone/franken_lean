# fln-mode-provenance: neutral
[package]
name = "fln-rt"
version = "0.0.0"
edition = "2024"
license = "LicenseRef-MIT-OpenAI-Anthropic-Rider"
publish = false

[dependencies]
fln-core = { path = "../fln-core" }
fln-unsafe-abi = { path = "../fln-unsafe-abi" }
fln-unsafe-region = { path = "../fln-unsafe-region" }
