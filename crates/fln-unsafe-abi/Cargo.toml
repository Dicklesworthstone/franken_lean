# fln-mode-provenance: neutral
[package]
name = "fln-unsafe-abi"
version = "0.0.0"
edition = "2024"
license = "LicenseRef-MIT-OpenAI-Anthropic-Rider"
publish = false

[dependencies]
fln-bignum = { path = "../fln-bignum" }
fln-libm = { path = "../fln-libm" }

# Test-only: k60n tree check in src/tests.rs. Rank 2 -> rank 0 is downward;
# production code cannot name fln_core. Declared in WORKSPACE_GRAPH.txt.
[dev-dependencies]
fln-core = { path = "../fln-core" }
